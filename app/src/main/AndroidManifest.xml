<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.xinze.xinze">

    <!-- if you want to load images from the internet -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- if you want to load images from a file OR from the internet -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <!-- 这个权限用于访问GPS定位 -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <!-- 用于访问wifi网络信息，wifi信息会用于进行网络定位 -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <!-- 照相机 -->
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".module.main.activity.MainActivity"
            android:theme="@style/TranslucentTheme" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".module.login.LoginActivity"
            android:theme="@style/TranslucentTheme" />
        <activity android:name=".module.register.RegisterActivity" />
        <activity android:name=".module.forget.ForgetPassWordActivity" />
        <activity android:name=".module.about.AboutUsActivity" />
        <activity android:name=".module.sysmsg.SystemMsgActivity" />
        <activity
            android:name=".module.certification.CertificationActivity"
            android:windowSoftInputMode="adjustPan|stateHidden" />
        <activity android:name=".module.find.FindGoodsActivity" />
        <activity android:name=".module.send.activity.SendGoodsActivity" />
        <activity android:name=".module.regular.activity.RegularRunActivity"/>
        <activity android:name=".module.order.OrderDetailActivity" />
    </application>

    </manifest>